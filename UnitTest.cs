namespace MovieLibrary_A4
{
    //Not completely understanding this yet 
    //not sure how i'd implement UnitTests into my program
    class UnitTest {
        /*
    [TestMethod]
        public void Debit_WithValidAmount_UpdatesBalance()
        {
        // Arrange
        bool isDuplicate = false;

        // Act
        Movie(isDuplicate);

        // Assert
        Assert.AreEqual(expected, "You cannot have a duplicate movie");
        }
        */
    }
}